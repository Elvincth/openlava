import React from "react";
import Header from "~/components/Header";

const createItem = () => {
  return (
    <div>
      <Header />
      This is createItem
    </div>
  );
};

export default createItem;
